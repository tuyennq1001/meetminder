# Repository workflow

- Mọi bug fix, feature và thay đổi mã nguồn phải được thực hiện trên một nhánh phụ, không commit trực tiếp vào `main`.
- Sau khi kiểm tra/build/test phù hợp, merge nhánh phụ đó vào `main` rồi mới push `main` nếu cần.
- Dùng tiền tố `codex/` cho nhánh phụ do Codex tạo, trừ khi người dùng chỉ định tên khác.
