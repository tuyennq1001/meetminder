{
  "name": "my-translator",
  "private": true,
  "version": "0.7.3",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:web": "npx serve src -p 3111",
    "lint": "cd src-tauri && cargo clippy",
    "format": "cd src-tauri && cargo fmt"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.10.0"
  }
}
